{
  "Header guard": {
    "prefix": "#once",
    "body": [
      "#pragma once",
      "#ifndef ${1}",
      "#define $1",
      "$0",
      "#endif // $1"
    ]
  },
  "lambda": {
    "prefix": "lambda",
    "body": [
      "[${1}](${2}){$0}"
    ]
  }
}
